# make relative link using user's selected directory as the location for the package root
# will need to do this for each item individaully
# must look for LISTOFFRAMES and if not available or empty we must expand image sequences using INPUTFIRSTFRAME, OUTPUTFIRSTFRAME with NUMBEROFFRAMES (and optionally INCREMENT, this defaults to 1)